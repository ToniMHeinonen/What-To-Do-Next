<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.tonimheinonen.whattodonext">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".voteactivity.VotingParentActivity"></activity>
        <activity android:name=".voteactivity.VoteWaitingActivity" />
        <activity android:name=".StartVoteActivity" />
        <activity android:name=".voteactivity.VoteLobbyActivity" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".voteactivity.VoteResultsActivity" />
        <activity android:name=".voteactivity.VoteTopActivity" />
        <activity android:name=".registration.ForgotPasswordActivity" />
        <activity android:name=".registration.LoginActivity" />
        <activity android:name=".registration.SignupActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".voteactivity.VoteSetupActivity" />
        <activity android:name=".ListsActivity" />
        <activity
            android:name=".InitializeActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>